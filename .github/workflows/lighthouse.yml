name: Lighthouse CI

on:
  workflow_dispatch: # Manual trigger only

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Lighthouse CI disabled — run manually when needed"
